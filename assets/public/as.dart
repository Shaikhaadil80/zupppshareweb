import 'package:flutter/material.dart';

class asd extends StatelessWidget {
  const asd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: ElevatedButton(child: Text('data'),onPressed: (){}),);
  }
}
